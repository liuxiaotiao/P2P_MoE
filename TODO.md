# 待办

> 新增了一条**跳过规划**的部署路径（`deploy/manual.py` + `deploy/run.py`）：
> 布局由人写，框架只做翻译与校验。下面这些条目讲的都是规划器那条主线；
> 手动放置那条路上，「维护层」「池合并」「通道一」这些一概不适用 ——
> 它本来就不承诺那些性质。

按依赖顺序排。每条写清楚**要动哪些文件**、**会破坏什么既有性质** —— 后者比前者
重要：这套方案的多数模块之间有真实的耦合，改一处往往要重估另一处的结论。

---

## ~~P0 · 真实模型执行层~~ · 部分完成

**已完成**：`runtime/torch_model.py` + `runtime/weights.py`，在真实 key 命名的微型
Qwen3-MoE checkpoint 上验证通过（21 项测试）。关键结论：**驻留集只要包含实际被
路由到的专家，输出与全量逐位一致** —— 「只驻留子集」不是近似。

**已完成（续）**：`runtime/node.py` 的 agent 已能接 torch 后端
（`backend="torch"` + `model_dir`），`examples/static_qwen.py` 在合成 checkpoint 上
端到端跑出 token。

**已完成（续 2）**：激活画像（`runtime/profile.py`）—— 全装跑一遍收逐层路由统计，
按覆盖率取驻留集。`deploy/run.py --profile-out` 采、`--profile` 用。
于是「后段只装 n_{u,l} 个专家」终于是**按真实激活选的**，不再是瞎选。

**还差**：
* **画像要多少 token 才稳** —— 现在没有收敛判据，采多久全凭手感。
  应该看逐层分布的 TV 距离随样本量的变化，稳定了就停；
* **前段并集** —— 有了逐 task 画像就能算 ∪_u S_{u,l}，前段不必再装全集。
  静态模式下省下来的内存直接换成更大的 L₀ 或更多通道；
* **采样策略** —— 现在后段 tail 是 argmax（`TorchSegmentModel.sample` 支持温度，
  但没接出来）。真实使用要 temperature / top-p / repetition penalty，
  以及一个跨请求可复现的种子口径；
* 对**真权重**验数值（合成 checkpoint 的权重是随机的，只验了机制）；
* ~~tokenizer~~ **已完成** —— `runtime/text.py`：编解码、增量解码（处理跨 token
  的半个 UTF-8 字符）、chat template（jinja2 渲染 checkpoint 自带的模板）、
  停止条件（generation_config 优先）。走的是独立的 `tokenizers` 包而不是
  transformers，因此控制机不需要 torch；节点侧只多了一个 `stop_ids` 整数列表；
* 其它模型家族的 adapter（现在只有 Qwen3-MoE 的 key 命名与 QK-norm）。

原始条目留档：

## P0 · 真实模型执行层（原始计划）

把 `PartialExpertMoEBlock` 从 numpy toy 换成 torch/HF，接真实 MoE 权重。

**先选对模型。** 跑 `python examples/model_fit.py` —— 判据是细粒度比
`n_experts / top_k`：**Mixtral 8x7B 不适合**（比值 4，单 token 就用掉 25% 的专家，
一个 task 的驻留集接近全集，「只驻留子集」省不下内存）。要选细粒度 MoE：
Qwen3-30B-A3B（128/8，比值 16）或 OLMoE-1B-7B（64/8，比值 8）。
这一条与显存多少无关，是模型自身的性质。

**动**：`runtime/model.py`（重写）、依赖清单（见下）。

**依赖该加哪些**（现在一个都没装，`test_requirements.py` 守着这条线）：

| 包 | 要不要 | 理由 |
|---|---|---|
| `torch` | 必须 | 张量与 GPU kernel |
| `safetensors` | 必须，且最关键 | 逐张量 mmap → 只打开需要的专家 key。pickle 格式的 `.bin` 做不到「只加载子集」，而那是整套方案的前提 |
| `transformers` | 大概率要 | **不是**为了它的前向（MoE block 得自己写），而是 tokenizer、config 解析、attention/RoPE/norm 这些逐模型的琐碎实现 |
| `accelerate` | **大概率不要** | 见下 |

**为什么大概率不用 accelerate**：它的两个主要用途在这套架构里都不适用。
`device_map="auto"` 的自动分层正是本方案要**替换掉**的东西 —— 放置由规划器决定、
写在 `NodePlan` 里，再让 accelerate 自动分一次只会打架；
`load_checkpoint_and_dispatch` 是整模块加载，它不认识「只加载第 20 层的 3、7、12 号
专家」，而我们要的恰恰是逐 key 过滤。唯一可能用得上的是 `init_empty_weights`
（meta 设备建骨架、只 materialize 需要的张量），但那也能用 torch 的 meta device
直接做。**落地时再决定，不预先写进依赖。**

另外这些依赖**只有跑推理的节点**需要 —— 控制机（规划、探测、下发）仍然只要 numpy。
到时候应该拆成 `requirements.txt`（控制机）+ `requirements-node.txt`（推理节点），
而不是一份给所有机器。

**不用动**：接口契约已经定死了 —— `forward(x, cache) -> (y, MoEStats)`、
`SegmentModel(cfg, layer_experts)`、`drop_kv(req)`。测试也是按契约写的
（`test_runtime.py` 里那批语义测试原样适用）。

**要点**：
* 选择性加载走 safetensors 的逐张量 mmap + 挑 key，`NodePlan.layers` 里的专家
  id 列表直接就是 key 的筛选条件；
* KV cache 用 HF 的 `Cache` 对象，按层切片实现「保前段、弃后段」；
* routing 要能拿到 logits 与 top-k 选择 —— 这是 fused MoE kernel 做不到、
  必须自己写 block 的原因。

**破坏**：toy 模型的 `--mem-cap-mb` 这类演示参数可以删掉；`sim/scenario.py` 里
按 MB 记的单位要换回 GB。

---

## ~~P0 · 权重分发~~ · 部分完成

**已完成**：`deploy/fetch.py` —— 按 `NodePlan` 算出需要的 key，读 safetensors
文件头拿到 `data_offsets`，用 HTTP Range 只下这些字节，本地拼成一个更小但合法的
checkpoint。15 台跑 Qwen3-30B-A3B 从 916GB 降到 179GB。`launch fetch` 批量推。
张量与上游逐位一致（`test_fetch.py` 逐个比对）。

**还差**：
* **增量补拉** —— 重规划后驻留集变了，现在是整份重拉。应该比对新旧 key 集合，
  只补差集。产出目录里已经有索引，做得到；
* **P2P 分发** —— 现在每台各自连上游。分散环境下节点之间带宽差异大，
  「谁已经有了谁供源」能省上游带宽，但要处理供源发现与校验；
* **断点续传** —— 一次 `fetch` 中途失败要从头再来。区间是幂等的，
  按区间落盘就能续；
* **校验** —— 现在只比对字节数。上游的 index 里通常有每个分片的 sha，
  拼完之后该按张量校验一次。

---

## P1 · 批处理（continuous batching）

**本轮明确推迟。** 记录一下现状与代价，将来重拾时不用重新想。

**现状**：并发度 = 前段条数。15 台节点建出 5 条前段 → 同时只能服务 5 条请求，
第 6 条排队（有界等待已实现）。

**文档立场**（I.2.4 注）：分散环境下 continuous batching 的收益被跨节点通信吞掉 ——
批内不同请求的 hidden state 需在同一跳内合并传输才划算，而分散链路的抖动使批内
对齐代价高、且破坏确定性延迟。文档选择排他独占 + 确定性延迟，代价是算力利用率低，
并明确写「若日后引入批处理，需重估 I.2.3 的均匀性可达性」。

**要动**：
* `runtime/model.py` —— `SegmentModel.forward` 现在是单请求的（KV 按 req 分桶但
  前向一次只跑一条）。要支持批内多请求就得改成 padded batch + 每请求的 KV 视图；
* `runtime/wire.py` —— 一条消息要能带多个请求的 hidden state（现在是一个 float32
  数组 + 一个 req 字段）；
* `runtime/node.py` —— 段内转发要做批的拆分与重组：不同请求可能在不同 decode 步；
* `runtime/coordinator.py` —— 空闲队列从「段的独占」变成「段上的 slot 计数」，
  `_release_front/_release_back` 的语义跟着变；
* `planner/types.py` —— I.2.2 的排他约束（一节点至多一条段）要放松，KV 的内存
  判据要按 batch_size × ctx_max 重算，这会直接改变分档估算与跳数下界。

**会破坏什么**（这才是重点）：
* **确定性延迟**。均匀性目标 A1″ 的前提是每条请求的路径固定、无排队。批内排队
  引入新的方差源，`spread_pairs` 不再只由放置决定；
* **零后悔定理**（III.3.1）。ε 界基于组合极差；批内等待时间进了总延迟之后，
  「任取空闲派发」与全知最优的差不再被 ε 界住；
* **命题 III.3.3 的论证**。「在线择优无利可图」的前提是放置差异被抖动淹没。
  加了批处理之后，批的组成成了新的可优化维度，那条论证要重做；
* **III.6.2 的无量纲比**。N_F/N_B ≥ 1 + W_id/S_rest 的推导假设逐请求配对占用，
  批处理下不成立。

**结论**：这不是加个 feature，是把方案的核心取舍翻过来。重拾时应该先回到 I.2.3
重估均匀性可达性，再决定实现路径。

---

## P1 · 通道一（SPRT）

现在只实现了通道二（后段 miss 率检出）。文档 II.5 的完整检出是双通道：

* 通道一（前段，统计）：decode 激活摘要照常捎带，tail(f) 序贯更新后验（SPRT）；
* 通道二（后段，结构）：滑窗 miss 率 > 基线×factor → 换绑。

**动**：`runtime/identify.py`（加 SPRT 状态机）、`runtime/node.py`（decode 时也
捎带前段直方图，现在只在 prefill 时报一次）、`runtime/coordinator.py`（两条通道
的判据合流）。

**要点**：现在换绑时「换到哪个池」用的是前段分类器的次优分数（一次性的）。
有了 SPRT 就能用序贯更新后的后验，判据更稳。

---

## P1 · 维护层（II.6 三层维护）

现在控制器只在采集能力时剔除连不上的节点，**运行中掉线没有重构逻辑**。

**动**：新增 `runtime/maintain.py`；`coordinator` 加健康检查与备胎顶替。

**三层**（文档 II.6）：
* 即时：节点掉线/劣化 → 受影响段就地重构，优先从备胎池顶替；若替换节点的正向
  表现仍落在公共带内 → 前段无需复验；
* 周期：偏离度累积或抖动画像漂移 → 该侧重跑两轮筛，重测公共带边界；
* 重规划：λ_u 大漂 / 池群结构变化 → 按新 λ_u 重配额重建后段，前段不动。

**要点**：备胎池已经在规划里产出了（Step 6 落选的前段），但**运行时还没有用起来**。
`DeploymentManifest.standby_fronts` 里有，协调器没读。

---

## P2 · 中继的横向扩展

`deploy/relay.py` 现在是单点：所有 hidden state 过一台机器。15 台的规模够用，
再大就要按段分流（每条通道一台中继），或者上 VPN overlay 恢复直连。

**还差**：多中继时的注册发现（节点该挂到哪台）、中继掉线后的重挂与在途请求处理。
现在中继挂了等于整池不可用 —— 与「协调器挂了在途请求照跑」的性质不同，
因为数据面本身就架在它上面。

---

## P2 · 去中心化的空闲队列（可选）

现在协调器持有全局空闲队列，tail(f) 识别完要问它要一条后段 —— 多一个控制面 RTT。
数据面已经是去中心化的（环在节点之间自己转，协调器挂了在途请求照跑）。

**若要做**：每条段自己宣告忙闲，客户端/前段抢租约。要处理竞争与脑裂。

**先确认需求**：现在这个 RTT 在控制面、不占 decode 每 token 预算。除非要去掉
控制器这个单点，否则收益不明显。

---

## P2 · 池合并（推论 III.7.4）

`planner/experts.py::merge_candidates` 已经能算出「哪两个 task 该合并」
（双向 q 都小 ⟺ 驻留集高度重叠 ⟺ 误判几乎无害且合并可省内存），但**没有执行路径**：
算出来只是打印，没人真的去合并两个池再重规划。

**动**：`planner/pipeline.py` 加一个合并后重跑的入口。

---

## P2 · 方向不对称的在线修正

真机探测只能量 RTT、取 RTT/2 作单向估计 —— 没有时钟同步就观测不到方向不对称
（`deploy/probe.py` 里有完整说明）。真正逐向的观测只能来自 II.6 的在线仪表：
decode 每 token 实付的 w 与 d_loop。

**动**：`coordinator` 已经收到每个 token 的上报，但没有拆出逐向的 w / d_loop
统计。加上之后可以回头修正规划期的对称假设。

---

## ~~P3 · drop-expert 的质量影响评估~~ · 工具已就绪

**已完成**：`examples/drop_expert_impact.py` —— 逐覆盖率量 miss 率、丢掉的门控
质量、输出分布 KL、贪心生成在第几步分叉。前段全装、只裁后段，与部署口径一致。

**已知的一条结论**（机制层面，与权重无关）：**贪心生成对扰动极其敏感**。
argmax 是离散决策，任何一步 top-1 被翻就整条轨迹分叉。所以
「prefill 偏差 1e-2」推不出「生成 64 token 没问题」。要判断质量看 KL，
要判断可复现性看分叉步。

**顺带做出来的**：`--miss-policy` 三选一（`drop` / `drop_noscale` / `local_topk`）。
合成权重上 `drop_noscale` 全程最好（KL 低 1.5–2 倍）—— 重归一会把低概率专家
放大到权重 1，而那不是路由的意思。默认仍是文档口径的 `drop`。

**这可能是第 10 处文档偏差**：II.5 明确写了「门控重归一」。如果真权重上
`drop_noscale` 仍然更好，该反馈给方案作者。**别拿合成权重的结论去改文档** ——
随机专家的输出是互不相关的随机方向，「用错」比「不用」更伤，真模型上未必如此。

**还差**：在**真权重 + 真语料**上跑出那张表，并且驻留集要来自**别的**语料统计的
画像（现在默认拿输入自己的路由当画像，是乐观上界）。真正要的数字是：
真实分布下覆盖率取多少，KL 才低到下游指标看不出差别。

---

## 需要反馈给方案作者的文档偏差

实现过程中发现 9 处（README「与文档的偏差」一节有完整论证与代码位置）。其中三处
值得优先核对，因为它们会改变算例的结论：

1. **算例 C.0 与 C.5 的专家重叠度自相矛盾** —— 并集 20（≈不重叠）与误绑 miss 率
   19% 不能同时成立；要让 q≈0.19，并集得是 13–14。这会改变前段每层的驻留量。
2. **II.5 的「基线 = 1−覆盖率」偏低 3–6 倍** —— 实测下来通道二会对绑对的池持续
   误报。三条原因：口径差一个 k、前段 miss 沿层放大、decode 分布漂移。
3. **III.5.2 的跳数下界不含供给约束** —— 单看它做 L₀ 准则会选出通道数更少的 L₀。
