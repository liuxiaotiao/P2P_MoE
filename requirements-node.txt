# 推理节点的额外依赖 —— **只有跑真实模型的节点需要**
#
#     pip3 install -r requirements-node.txt
#
# 控制机（跑规划、探测、下发清单）**不需要**这些：它只要 requirements.txt 的 numpy。
# 这不是洁癖 —— 控制机上装 torch 意味着多几 GB 的镜像和一堆 CUDA 依赖，而它一个
# 张量都不碰。
#
# 想只验证部署路径（toy MoE，numpy 前向）的话，节点上也不需要装这些。

-r requirements.txt

torch>=2.1              # 张量与 GPU kernel
safetensors>=0.4        # 逐张量 mmap —— 「只加载点名的专家」全靠它
                        # pickle 的 .bin 做不到，那是一整个序列化流

# transformers 暂时**没有**列入：MoE block 我们自己写（fused kernel 拿不到 routing、
# 也不支持专家子集），attention / RoPE / RMSNorm 也已经在 torch_model.py 里实现了。
# 真正需要它的时候是接 tokenizer —— 到那一步再加：
# transformers>=4.40
#
# accelerate 不需要，理由见 requirements.txt 的注释与 TODO.md 的 P0 条目。
